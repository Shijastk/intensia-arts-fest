<svg viewBox="0 0 100 100" fill="none" xmlns="http://www.w3.org/2000/svg">
  <!-- The circle of 'a' (Red for high contrast) -->
  <circle cx="45" cy="55" r="25" fill="#EF476F" />
  
  <!-- The stem of 'a' -->
  <rect x="60" y="30" width="15" height="50" rx="7.5" fill="#23B5D3" />
  
  <!-- Decorative dot (top right) -->
  <circle cx="80" cy="20" r="10" fill="#FFD166" />
  
  <!-- Decorative dot (bottom left) -->
  <circle cx="20" cy="80" r="8" fill="#FF90E8" />
</svg>
